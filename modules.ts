module Utility {
    export class Useful {
        timesTwo(n : number) {
            return n * 2;
        } 
    } 
}

//modules export classes, it can be accessed by any file witch requires that module
