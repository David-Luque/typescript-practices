//al of this are required parameters
//whit "?" were are defining that property as not required when using this interface
function printName(stark) {
    console.log(stark.name);
}
printName({ name: "Eddard" });
printName({ name: "Joe" });
